# DAA Lab - Question 3
## Convolution Operation on Vectors

### Problem

Given two vectors `A` and `B` of lengths `m` and `n`, respectively, compute their convolution vector `C`.

The convolution is defined as:

```text
C[k] = Σ A[j] B[k-j]